diff --git a/src/main/ipc/models.ts b/src/main/ipc/models.ts
index 64d3b4b..435c11f 100644
--- a/src/main/ipc/models.ts
+++ b/src/main/ipc/models.ts
@@ -343,9 +343,12 @@ export function registerModelHandlers(ipcMain: IpcMain): void {
         modified_at?: string
       }> = []
 
-      // Recursively read directory
-      async function readDir(dirPath: string, relativePath: string = ''): Promise<void> {
-        const entries = await fs.readdir(dirPath, { withFileTypes: true })
+      // Import fs - will be replaced with wasabio at build time
+      const fsSync = await import('fs')
+
+      // Recursively read directory using sync API (wasabio is in-memory)
+      function readDir(dirPath: string, relativePath: string = ''): void {
+        const entries = fsSync.readdirSync(dirPath, { withFileTypes: true }) as any[]
 
         for (const entry of entries) {
           // Skip hidden files and common non-project files
@@ -361,9 +364,9 @@ export function registerModelHandlers(ipcMain: IpcMain): void {
               path: '/' + relPath,
               is_dir: true
             })
-            await readDir(fullPath, relPath)
+            readDir(fullPath, relPath)
           } else {
-            const stat = await fs.stat(fullPath)
+            const stat = fsSync.statSync(fullPath)
             files.push({
               path: '/' + relPath,
               is_dir: false,
@@ -374,7 +377,7 @@ export function registerModelHandlers(ipcMain: IpcMain): void {
         }
       }
 
-      await readDir(workspacePath)
+      readDir(workspacePath)
 
       // Start watching for file changes
       startWatching(threadId, workspacePath)
